import {createContext} from 'react'

//paso 1 la variable se puede nombrar con mayuscula
export const UserContext = createContext({}); //  el input de createContext es el valor por defecto. En este caso un object vacio